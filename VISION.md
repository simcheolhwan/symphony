# 이슈에서 머지까지

웹 서비스를 만드는 개발자가 Symphony와 일하는 방식이다. 구현과 리뷰 피드백 대응은 에이전트가 알아서 한다. 사람은 판단하고 마무리한다. 이 문서는 이슈 하나가 구현과 피드백 반영을 거쳐 머지에 이르는 경로만 다루고, 동료의 PR을 리뷰하는 일은 다루지 않는다. 경로는 작업마다 하나씩 시작되고, 여러 작업의 경로가 서로 다른 단계에서 동시에 진행된다. 각 단계는 🧑‍💻 사람의 행동과 ⚙️ 그 뒤에서 실행되는 시스템을 나란히 보여준다.

## 위임 — 이슈를 다듬어 맡긴다

### 🧑‍💻 사람

- 맡길 이슈 두 개를 에이전트와의 인터뷰로 다듬는다. ① 목록 화면 필터에 검색 입력 추가, ② 목록 CSV 내보내기.
- 인터뷰에서 에이전트가 사실을 스스로 조사하고 미결정 사항만 물으면 사람은 결정만 내린다. 인터뷰가 끝나면 에이전트가 합의된 명세를 이슈 설명에 반영한다.
- 다듬은 이슈를 `Todo`로 올리고 자신에게 할당한 뒤 `agent` 레이블을 붙여 위임한다.
- 이제 사람의 일로 돌아간다. 설계 초안 정리, 다음 명세 작성, 동료와의 논의.

### ⚙️ 시스템

- 폴링 중이던 인스턴스가 두 이슈를 발견해 디스패치한다.
- 이슈마다 작업 폴더를 만들고 저장소를 클론해 에이전트를 기동한다.
- 디스패치와 동시에 Slack 채널에 작업 메시지를 하나씩 게시한다. 이 작업의 이후 소식은 전부 그 스레드에 답글로 게시한다. 실제로 어떤 메시지가 게시되는지는 [`notifier/README.md`](notifier/README.md)의 스레드 예시 절이 보여준다.
- 에이전트는 이슈 코멘트(Workpad)에 계획을 적는다 → 이슈를 `In Progress`로 옮긴다 → 최신 `origin/main`과 맞춘 브랜치에서 구현을 시작한다.

## 구현 — Draft PR까지

### ⚙️ 시스템

- 이슈 ①의 구현이 끝나면 검증한다. 브라우저로 화면을 직접 열어 검색이 동작하는지 확인한다.
- UI가 바뀌었으니 핵심 시나리오를 동영상으로 녹화해 PR 검증 결과에 임베드한다.
- 코드 리뷰를 실행하고 지적 사항이 사라질 때까지 수정을 반복한다.
- 커밋 → Draft PR 생성 → 이슈를 `In Review`로 옮긴다. 여기까지가 이 에이전트의 몫이다. 스레드에 완료 답글을 게시한다.

### 🧑‍💻 사람

- Slack 완료 알림을 보고 Linear에서 `In Review`로 넘어온 이슈를 연다.
- PR을 열어 코드보다 동영상을 먼저 보고, 검색이 의도대로 동작하는지 확인한다.
- 방향이 맞으면 Draft를 ready로 바꾼다 → CODEOWNERS가 리뷰어를 자동 지정한다.

## 관측 — 진행은 Slack 스레드에 쌓인다

### 🧑‍💻 사람

- 자리를 비운 사이에도 폰에서 Slack 스레드만 훑으면 작업별 진행이 보인다. 재시도가 반복되는 작업은 답글이 쌓여 눈에 띈다.
- 알림은 유실을 허용하는 보조 채널이다. 작업 상태의 원본은 트래커다.

## 피드백 대응 — 응답과 재요청의 왕복

동료가 내 PR을 리뷰해 지적 사항을 남기면 왕복이 시작된다.

### ⚙️ 시스템

- 내 PR에 답 없는 리뷰 지적 사항이 남으면 저자 에이전트가 이어받는다.
- 맞는 지적 사항은 고쳐서 푸시한다. 아니라고 판단한 지적 사항에는 근거를 답글로 남긴다. UI 수정이면 동영상도 다시 붙인다.
- 푸시는 리뷰봇을 다시 깨운다. 봇이 새로 지적하면 그것부터 처리하고, 봇이 조용해진 뒤에야 동료에게 리뷰를 재요청한다.
- 모든 지적 사항에 응답을 남기고, 대응을 마친 리뷰는 재요청한다.
- 재요청을 받은 동료가 다시 리뷰한다. 이 왕복은 남은 지적 사항이 없어질 때까지 반복되고, 그때부터는 사람의 최종 리뷰만 남는다.

## 차단 — 막히면 스스로 멈추고 사람을 부른다

### ⚙️ 시스템

- 이슈 ②의 에이전트가 구현 중 미결정 사항을 만난다. 합리적인 가정으로도 안전하게 구현할 수 없으면 Workpad에 사유를 적고 차단을 기록한다.
- 막히자마자 스스로 `agent` 레이블을 떼어 무의미하게 반복될 재시도를 중단한다.
- 스레드에 차단 답글을 게시한다. 답글에는 차단 사유와 에이전트 요약이 함께 실린다.

### 🧑‍💻 사람

- 차단 답글은 작업이 막힌 시점에 도착한다. Workpad를 열지 않아도 Slack 답글에서 사유를 읽는다. "대용량 목록을 즉시 다운로드로 내보낼지 이메일 발송으로 처리할지 정해지지 않았다."
- 이슈에 결정을 답글로 남긴다 → 레이블을 다시 붙인다 → 다음 폴링에서 작업이 이어진다.

## 머지 — 사람의 마무리

### 🧑‍💻 사람

- 쌓인 PR을 훑는다. 리뷰 스레드는 전부 답변돼 있고 검증 동영상이 붙어 있다.
- 최종 리뷰는 방향과 취향의 판단에 집중한다.
- 스쿼시 머지 → 다음에 맡길 이슈를 Backlog에서 골라 둔다.

### ⚙️ 시스템

- 연결된 PR의 머지를 감지한 Linear가 이슈를 `Done`으로 옮긴다.
- 오케스트레이터는 끝난 이슈의 에이전트를 멈추고 작업 폴더를 정리한다.
- 인스턴스들은 폴링을 계속한다. 레이블이 붙는 순간 다시 움직이기 위해.

## 사람에게 남는 일

자동화 이후에도 다음은 사람이 맡는다.

- 이슈를 다듬고 맡기는 일, Draft를 ready로 전환하는 일.
- 최종 리뷰와 스쿼시 머지.
- 막힌 작업에 결정을 내려 주는 일.

이 경로의 설계 원리는 하나다. 감독자 없는 반복은 스스로 멈추게 만든다. 에이전트는 응답을 남겨서 멈추고, 막히면 레이블을 떼어서 멈춘다. 사람은 그 멈춘 지점만 이어 붙인다.
